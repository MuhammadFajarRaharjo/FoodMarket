<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.belajar.foodmarket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodMarket">
        <activity
            android:name=".presentasion.auth.AuthActivity"
            android:exported="false"
            android:theme="@style/Theme.FoodMarket.NoActionBar" />

        <activity
            android:name=".presentasion.main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.FoodMarket.NoActionBar" />

        <activity
            android:name=".presentasion.SplashScreenActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FoodMarket.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>